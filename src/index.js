// don't actually have to import react to any updated react app
//but this shows we are using react when using JSX code
import React from 'react';

import ReactDOM from 'react-dom';

import './index.css';
import App from './App';

ReactDOM.render(<App />, document.getElementById('root'));
